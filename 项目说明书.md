# CareerMate 项目说明书

> 此文件供 AI 助手快速了解项目全貌，无需重复扫描代码库。
> 最后更新：2026-07-19

## 项目定位

CareerMate 是面向高校生与职场新人的 **AI 职业导航与终身学习伙伴系统**，基于蚂蚁百宝箱企业版 AI。通过对话式画像采集、职业能力评估、灵活周期路径规划、月度行动计划、职场模拟训练、资源推荐和长期记忆管理，帮助用户将抽象职业目标拆解为可执行、可追踪、可调整的成长路径。

为**浙江大学学生服务创新大赛**开发的本地原型，目标是初赛冲奖。

**核心特色：**
- 支持**任意职业方向**，不限于预设种子岗位。已知职业通过别名匹配模板，未知职业自动生成 stable custom key（SHA-256 前12位 hex）并联网调研
- **Plan V2 灵活计划**：不强制固定年/季/月结构，AI 根据职业特点自由设定 horizon、phases 和 actions；V1 固定 36 月结构仅用于历史兼容读取
- **AgentResponse 协议**：结构化 AI 输出（intent/task/questions/operations/sourceRefs），画像/计划/记忆写入需用户显式确认，正文零副作用
- **三级 AI 降级**：api → manual → mock，mock 模式零外部依赖即可运行完整流程
- **有状态轮次服务**：幂等 clientRequestId + 会话锁 + 两阶段事务，防止并发竞态和重复处理

## 技术栈

| 领域 | 选型 | 版本 |
|------|------|------|
| 框架 | Next.js (App Router) | ^16.2.10 |
| 语言 | TypeScript（严格模式） | ^5.9.2 |
| 前端 | React | ^19.2.4 |
| 样式 | Tailwind CSS v4 | ^4.1.12 |
| 字体 | Geist / Geist Mono | 内置于 next/font |
| 图标 | lucide-react | ^0.468.0 |
| 图表 | recharts（能力雷达图） | ^3.1.2 |
| Markdown | react-markdown + remark-gfm | ^10.1.0 / ^4.0.1 |
| 工具函数 | clsx + tailwind-merge | ^2.1.1 / ^2.5.5 |
| ORM/数据库 | Prisma + SQLite（单文件本地） | ^6.14.0 |
| 运行时校验 | Zod | ^3.24.1 |
| 认证 | 自定义 Session Token（bcryptjs + SHA-256 cookie） | ^3.0.2 |
| AI 接入 | 蚂蚁百宝箱 API（api/manual/mock 三级降级） | - |
| 单元测试 | Vitest | ^3.2.6 |
| E2E 测试 | Playwright | ^1.61.1 |
| 开发工具 | ESLint + tsx（运行 TypeScript 脚本） | ^9.35.0 / ^4.20.5 |

## 快速开始

```bash
# 安装依赖
npm.cmd install

# 配置环境变量（默认 mock 模式可直接使用）
copy .env.example .env.local

# 初始化数据库
npm.cmd run prisma:generate
npm.cmd run db:migrate:deploy
npm.cmd run seed

# 启动开发服务器
npm.cmd run dev
# → http://localhost:3000
```

## 演示账号

密码均为 `careermate123`（bcryptjs 哈希存储，salt rounds=10）：

| 账号 | 显示名 | 身份 | 目标岗位 | 可投入时间 |
|------|--------|------|---------|-----------|
| `student_lin` | 小林 | 大三·数字媒体技术 | AI 产品经理 | 每周 6h |
| `student_chen` | 小周 | 大二·统计学 | 数据分析师 | 每周 8h |
| `student_wu` | 小陈 | 大三·新闻传播 | AIGC 内容运营 | 每周 5h |
| `worker_zhao` | 阿敏 | 职场新人·运营 1 年 | AI 产品经理（转岗） | 每周 3h |
| `career_switch_li` | 宇航 | 新媒体运营 | 数据分析师（摇摆中） | 每周 7h |
| `admin` | 管理员 | - | AI 产品经理 | 管理员（审核岗位草稿） |

种子数据包含：6 个用户（5 普通 + 1 管理员）、3 个 RoleTemplate（AI 产品经理/数据分析师/AIGC 内容运营）、6 条学习资源、3 个 ManualAiSample（计划生成 mock 数据）、1 个 RoleDraft（AI 项目助理演示）、每个普通用户 1 个 V1 CareerPlan + 1 条 MemoryItem + 1 条 ProgressLog。

## AI 模式

| 模式 | 说明 | 外部依赖 |
|------|------|---------|
| **mock**（默认） | 本地确定性 fixture 数据，无需外部依赖即可运行完整流程 | 无 |
| **api** | 真实百宝箱 API（chat/create + WebSocket SSE 流式 + datasets/retrieve 知识库检索） | API-Key + Agent ID |
| **manual** | 手工复制的样本数据（ManualAiSample 表），静态 Fixture | 无（需预先录入样本） |

降级链：`api → manual → mock`。每次 AI 调用通过 `AiExecutionMeta` 记录 `requestedMode`（请求模式）、`actualMode`（实际模式）、`degraded`（是否降级）、`fallbackReason`（降级原因）。

## 环境变量与 Feature Flag

核心环境变量（完整定义见 `.env.example`）：

```bash
# ── 数据库 ──
DATABASE_URL="file:./dev.db"

# ── 百宝箱连接 ──
TBOX_MODE="mock"                          # mock | api | manual
TBOX_API_KEY="placeholder"
TBOX_APP_ID="replace-with-app-id"
TBOX_AGENT_ID="replace-with-agent-id"
TBOX_AGENT_VERSION=""
TBOX_SEARCH_ENGINE="false"
TBOX_RETRIEVAL_MODE="agent"              # agent | hybrid

# ── 知识库数据集 ID ──
TBOX_DATASET_ROLE_COMPETENCY=""
TBOX_DATASET_LEARNING_RESOURCES=""
TBOX_DATASET_SIMULATION_SCENES=""
TBOX_DATASET_ETHICS_RULES=""
TBOX_DATASET_CAREER_TRENDS=""

# ── 传输决策（fail-closed 安全默认值）──
TBOX_HISTORY_MODE="context_only"          # context_only | provider
TBOX_CONTEXT_TRANSPORT="question_prefix"  # question_prefix | business_data
TBOX_STRUCTURED_MODE="disabled"           # disabled | terminal（⚠️ 生产不可用：平台能力缺口）
TBOX_REUSE_REMOTE_CONVERSATION_ID="false"
TBOX_PROBE_AGENT_ID=""

# ── 百宝箱端点 ──
TBOX_CHAT_ENDPOINT="https://o.tbox.cn/openapi/v1/chat/create"
TBOX_RETRIEVE_ENDPOINT="https://api.tbox.cn/api/datasets/retrieve"
TBOX_WS_URL="wss://o.tbox.cn/ws/chat"
TBOX_STREAM_TIMEOUT_MS="90000"

# ── Feature Flag（安全默认值，fail-closed 原则）──
OPEN_CHAT_ENTRY="true"                    # 开放聊天入口（false 回滚到旧 onboarding 引导）
STATEFUL_CHAT_TURNS="true"               # 有状态轮次服务（幂等+会话锁+两阶段事务）
AGENT_OPERATIONS_V1="false"              # Agent Operations（profile_patch/memory_proposal/plan_draft 自动执行）
PLAN_V2_WRITE="false"                    # Plan V2 灵活周期写入（关闭时仍可读取 V1/V2）
CONVERSATION_SUMMARY="false"             # 版本化会话摘要（24 条消息阈值触发）

# ── MCP/插件认证 ──
CAREERMATE_PLUGIN_TOKEN="placeholder"
CAREERMATE_PLUGIN_USER_ID="placeholder"   # 服务端绑定，不接受工具参数覆盖
CAREERMATE_PLUGIN_SCOPES="profile:read,profile:candidates,courses:read,jobs:read,progress:write"
ALLOW_UNAUTHENTICATED_PLUGIN="false"
```

### Feature Flag 详解

| Flag | 默认 | 说明 | 依赖条件 |
|------|------|------|---------|
| `OPEN_CHAT_ENTRY` | **true** | 首页直接进入聊天，不强制走 onboarding 引导 | 无 |
| `STATEFUL_CHAT_TURNS` | **true** | 幂等请求 + 会话级锁 + 两阶段事务（begin/commit），支持 replay | 无 |
| `AGENT_OPERATIONS_V1` | **false** | AI 可自动提议画像 patch / 记忆提案 / 计划草稿并待用户确认 | ⚠️ 生产不可用：需 TBox structured=terminal + TBOX_MODE=mock |
| `PLAN_V2_WRITE` | **false** | 允许 AI 生成灵活周期/阶段 Plan V2（关闭时仍可读取已有 V2 计划） | 需真实探针验证 |
| `CONVERSATION_SUMMARY` | **false** | 24 条消息阈值触发版本化摘要压缩 | 需真实探针验证 |

## 项目结构

```
CareerMate/
├── src/                                # 252 个源文件（.ts / .tsx）
│   ├── app/                            # Next.js App Router 页面与 API 路由
│   │   ├── layout.tsx                  #   根布局（Geist 字体 + lang="zh-CN"）
│   │   ├── globals.css                 #   全局样式（Tailwind v4 + 设计令牌 + 聊天组件样式）
│   │   ├── page.tsx                    #   首页 /（未登录→/login；已登录→聊天首页 ChatHomePage）
│   │   ├── login/page.tsx              #   登录/注册页面
│   │   ├── onboarding/page.tsx         #   对话式画像引导页面
│   │   ├── dashboard/page.tsx          #   成长仪表盘页面
│   │   ├── path/page.tsx               #   职业路径页面
│   │   ├── simulation/page.tsx         #   模拟训练页面
│   │   ├── resources/page.tsx          #   资源中心页面
│   │   ├── memory/page.tsx             #   记忆与权限中心页面
│   │   ├── chat/page.tsx               #   旧聊天页面（已废弃，重定向至 /）
│   │   ├── admin/page.tsx              #   管理员岗位草稿审核页面
│   │   └── api/                        #   全部 API Route Handlers（Next.js Route Handlers）
│   │       ├── auth/                   #     POST 登录/注册/登出
│   │       ├── chat/conversations/     #     CRUD 会话 + SSE 流式消息 + 决策端点
│   │       ├── plans/                  #     职业计划 CRUD + 生成 + 重规划 + 接受/确认决策
│   │       ├── careers/                #     职业探索报告（生成/提交）
│   │       ├── profile/                #     画像读写 + 候选（candidates）增删改
│   │       ├── onboarding/             #     引导对话 + 完成标记
│   │       ├── mcp/                    #     JSON-RPC 2.0 MCP 端点（initialize/tools_list/tools_call）
│   │       ├── tbox/                   #     百宝箱聊天/流式/检索（服务端转发）
│   │       ├── resources/              #     学习资源查询
│   │       ├── simulations/            #     模拟训练会话（消息/完成）
│   │       ├── memory/                 #     记忆条目增删改 + 开关 + 决策
│   │       ├── admin/                  #     角色草稿生成/审核/驳回
│   │       ├── privacy/                #     数据导出（GDPR 风格 ZIP）/ 清空（需确认短语）
│   │       └── me/                     #     当前用户聚合数据（WorkspaceData）
│   │
│   ├── components/                     # 可复用 React 组件
│   │   ├── chat/                       #   聊天系统组件
│   │   │   ├── chat-home.tsx           #     聊天首页（会话列表 + 消息区 + 输入框，替代旧 ChatView）
│   │   │   ├── chat-thread.tsx         #     消息线程渲染（支持实时流式更新）
│   │   │   ├── chat-composer.tsx       #     输入框 + 快捷操作
│   │   │   ├── conversation-sidebar.tsx #    会话侧栏（新建/切换/删除）
│   │   │   ├── memoized-markdown.tsx   #     Markdown 渲染（memo 优化）
│   │   │   ├── message-parts.tsx       #     消息部件渲染（text/citations/cards）
│   │   │   ├── citation-list.tsx       #     引用来源列表
│   │   │   ├── quick-actions.tsx       #     快捷操作按钮
│   │   │   ├── plan-summary-card.tsx   #     计划摘要卡片
│   │   │   ├── profile-candidate-card.tsx  # 画像候选确认卡片
│   │   │   ├── memory-proposal-card.tsx    # 记忆提案卡片
│   │   │   ├── exploration-report-card.tsx # 职业探索报告卡片
│   │   │   └── growth-profile-drawer.tsx   # 成长画像抽屉面板
│   │   ├── shell/                      #   应用壳（布局框架）
│   │   │   ├── app-shell.tsx           #     主布局壳（旧版，逐步迁移）
│   │   │   ├── product-sidebar.tsx     #     产品侧栏（桌面端 + 移动端抽屉）
│   │   │   ├── mobile-navigation.tsx   #     移动端底部导航
│   │   │   ├── nav-items.ts           #     导航项配置（5 个主功能 + 1 个 Admin + 返回聊天）
│   │   │   └── page-header.tsx        #     页面标题栏（含 AI 运行时状态徽章）
│   │   ├── ui/                         #   基础 UI 组件（类 shadcn 风格）
│   │   │   ├── button.tsx / cn.ts / confirm-dialog.tsx / empty-state.tsx
│   │   │   ├── field.tsx / inline-alert.tsx / skeleton.tsx / status-badge.tsx / surface-card.tsx
│   │   ├── workspace.tsx               #   工作台总控制器（数据加载 + 视图分发 + URL 路由匹配）
│   │   ├── workspace-page.tsx          #   工作台页面包装器（鉴权 + 管理员判断）
│   │   └── login-form.tsx              #   登录/注册表单
│   │
│   ├── features/                       # 页面级功能视图（纯客户端组件）
│   │   ├── chat/chat-view.tsx          #   旧聊天视图（已废弃，功能迁移至 chat-home.tsx）
│   │   ├── dashboard/dashboard-view.tsx #  成长概览（指标卡 + 能力雷达图）
│   │   ├── onboarding/onboarding-view.tsx
│   │   ├── path/path-view.tsx
│   │   ├── simulation/simulation-view.tsx
│   │   ├── resources/resource-view.tsx
│   │   ├── memory/memory-view.tsx
│   │   └── admin/admin-view.tsx
│   │
│   └── lib/                            # 核心业务逻辑（服务端 + 客户端共享）
│       ├── auth.ts                     #   Cookie 会话管理（登录/验证/登出）
│       ├── prisma.ts                   #   Prisma 客户端单例（开发环境全局缓存）
│       ├── env.ts                      #   环境变量读取 + Feature Flag 判断（fail-closed 默认）
│       ├── types.ts                    #   核心类型定义（6 维能力 / Plan V1&V2 / DTO / UnifiedPlan）
│       ├── workspace-types.ts          #   工作台聚合数据类型 WorkspaceData
│       ├── dto.ts                      #   数据库行 → API 响应 DTO 转换
│       ├── client-api.ts              #   前端 API 封装（fetchApi / fetchStream）
│       ├── career.ts                   #   匹配度计算 + 计划构建
│       ├── path.ts                     #   计划时间线分组
│       ├── resources.ts               #   资源来源过滤
│       ├── onboarding.ts              #   画像引导逻辑
│       ├── onboarding-transcript.ts   #   引导对话转录
│       ├── onboarding-resume.ts       #   引导恢复逻辑（刷新后继续未完成引导）
│       ├── onboarding-routing.ts      #   引导路由判断
│       ├── simulation.ts              #   模拟训练逻辑（旧版，逐步迁移至 features/simulation/）
│       ├── privacy.ts                 #   隐私数据导出/清理（含确认短语校验）
│       ├── ai-runtime.ts              #   AI 运行时状态管理
│       ├── markdown-utils.ts          #   Markdown 规范化（修复加粗空格等）
│       ├── session-security.ts        #   会话安全加固
│       ├── plugin-auth.ts             #   插件/MCP 认证（Token 验证 + Scope 白名单）
│       ├── json.ts                    #   JSON 解析/序列化工具
│       ├── profile-defaults.ts        #   画像默认值
│       ├── api.ts                     #   API 通用工具
│       ├── admin-role-draft.ts        #   管理员角色草稿逻辑
│       ├── tbox.ts                     #   百宝箱适配层 Barrel 导出（聚合公开 API）
│       ├── chat/                      #   聊天服务层（核心子系统）
│       │   ├── types.ts               #     聊天类型（SafeCareerContext / CareerChatIntent 等）
│       │   ├── agent-protocol.ts      #     AgentResponse 协议定义（Zod Schema + 4 种 AgentOperation）
│       │   ├── context.ts             #     意图分类（关键词匹配）+ 安全上下文裁剪
│       │   ├── context-builder.ts     #     上下文构建器（profile/plan/memory scope 控制）
│       │   ├── conversation-state.ts  #     会话状态机
│       │   ├── service.ts             #     聊天业务逻辑
│       │   ├── server.ts              #     SSE 流式服务器适配
│       │   ├── sse.ts                 #     SSE 工具（事件写入）
│       │   ├── stream-service.ts      #     流式处理主服务（有状态/无状态双路径）
│       │   ├── turn-service.ts        #     轮次管理（幂等 begin + commit + replay）
│       │   ├── persistence.ts         #     持久化层
│       │   ├── repository.ts          #     数据访问层
│       │   ├── schemas.ts             #     Zod 校验
│       │   ├── artifact-service.ts    #     结构化卡片生成
│       │   ├── artifacts.ts           #     消息部件类型定义
│       │   ├── question-ledger.ts     #     提问账本（防重复提问）
│       │   └── summary-service.ts     #     会话摘要服务（版本化，24 条阈值）
│       ├── tbox/                      #   百宝箱适配层
│       │   ├── types.ts               #     TboxConfig / TboxMessage 等核心类型
│       │   ├── adapter.ts             #     核心适配器
│       │   ├── client.ts              #     HTTP 客户端
│       │   ├── streaming.ts           #     流式处理（WebSocket SSE 消费）
│       │   ├── sse.ts                 #     SSE 帧解析（基于真实 API 事件格式重写）
│       │   ├── frontend-sse.ts        #     前端 SSE 消费（EventSource + fetch 兜底）
│       │   ├── normalization.ts       #     响应归一化（多字段变体兼容）
│       │   ├── errors.ts              #     错误类型
│       │   ├── result.ts              #     结果累加器（去重 + 完整性校验）
│       │   ├── citations.ts           #     引用处理（去重 + 截断 max=12）
│       │   ├── retrieval.ts           #     知识库检索
│       │   ├── schemas.ts             #     Zod schema
│       │   ├── fixtures.ts            #     Mock Fixture 数据
│       │   ├── plan.ts                #     AI 计划生成
│       │   ├── probe-judge.ts         #     探针判定逻辑
│       │   ├── structured-result.ts   #     结构化结果解析
│       │   ├── capabilities.ts        #     能力定义
│       │   └── capability-schemas.ts  #     能力 Zod Schema
│       ├── plans/                     #   计划服务
│       │   ├── schema-v2.ts           #     Plan V2 灵活周期 Zod Schema（horizon/phases/tasks/actions）
│       │   ├── compatibility.ts       #     V1/V2 双向兼容（V2→V1 数组投影）
│       │   ├── generation-service.ts  #     计划生成服务
│       │   └── replan-service.ts      #     重规划服务
│       ├── profile/                   #   画像服务
│       │   ├── candidate-service.ts   #     画像候选服务（白名单字段控制）
│       │   ├── ability-evidence.ts    #     能力证据服务
│       │   ├── profile-mutation-service.ts # 画像变更服务
│       │   └── profile-patch.ts       #     画像补丁逻辑
│       ├── careers/                   #   职业探索服务
│       │   ├── exploration-service.ts
│       │   └── exploration-schema.ts
│       ├── simulation/                #   模拟训练服务
│       │   └── generation.ts          #     场景生成
│       ├── memory/                    #   记忆服务
│       │   └── proposal-service.ts    #     记忆提案服务
│       ├── roles/                     #   角色身份
│       │   └── identity.ts            #     角色身份解析（种子别名 + 数据库别名 + 自定义 SHA-256 key）
│       └── tools/                     #   MCP 工具注册表
│           ├── registry.ts            #     通用工具注册框架（基于 Zod + scope 白名单）
│           └── careermate-registry.ts #     CareerMate 5 工具注册（profile/courses/jobs/progress）
│
├── prisma/
│   ├── schema.prisma                  #   SQLite，19 个数据模型
│   ├── seed.ts                        #   种子数据（6 用户 + 3 岗位模板 + 6 资源 + 其他）
│   ├── dev.db                         #   开发数据库（SQLite 单文件）
│   └── migrations/                    #   8 个数据库迁移
│       ├── 20260710000000_baseline                    # 初始基线（User/Profile/Plan/Resource 等核心表）
│       ├── 20260710010000_p0_foundation               # P0 基础：ProgressLog/Memory/Simulation/Candidate/Draft
│       ├── 20260711154410_chat_first_foundation       # 聊天基础：ChatConversation/ChatMessage
│       ├── 20260713194446_remote_conversation_ids     # 远端会话 ID 追踪
│       ├── 20260715100000_open_chat_state             # 开放聊天状态机字段
│       ├── 20260715165206_nullable_profile_fields     # 画像字段可空（第一版）
│       ├── 20260716000000_nullable_profile_fields     # 画像字段可空（第二版修正）
│       └── 20260716141459_add_operation_execution     # OperationExecution 幂等操作表
│
├── docs/                              # 项目文档
│   ├── AI职业导航项目方案.md           #   项目整体方案
│   ├── 产品需求文档_PRD.md             #   产品需求文档
│   ├── 接口设计文档.md                 #   API 接口设计
│   ├── evaluation/                    #   评测文档
│   │   ├── competition-evidence.md    #     比赛佐证材料
│   │   ├── tbox-cases.md              #     百宝箱评测用例
│   │   └── user-testing.md            #     用户测试计划
│   └── tbox/                          #   百宝箱配置文档
│       ├── main-agent.md              #     主 Agent 配置（AgentResponse 协议 + Plan V2 方案）
│       ├── workflows.md               #     工作流配置（7 类结构化输出）
│       ├── knowledge-bases.md         #     知识库说明
│       ├── acceptance-evidence.md     #     百宝箱真实 API 验收证据
│       ├── 百宝箱配置清单.md           #     百宝箱完整配置清单
│       ├── 2026-07-14-新主Agent适配修复优化计划.md
│       └── 2026-07-15-开放式主聊天与混合记忆实施计划.md
│
├── e2e/                               # Playwright E2E 测试（4 spec，43 条用例）
│   ├── chat-home.spec.ts              #   聊天首页全流程（17 条）
│   ├── p0-flows.spec.ts               #   P0 核心流程（6 条）
│   ├── unified-shell.spec.ts          #   统一壳导航 + 各视图渲染（11 条）
│   └── chat-context-continuity.spec.ts #  上下文连续性与记忆提案（9 条）
│
├── scripts/                           # 辅助脚本
│   ├── e2e-server.mjs                 #   E2E 测试专用服务器
│   ├── migration-smoke.mjs            #   迁移冒烟测试
│   ├── secret-scan.mjs                #   敏感信息扫描（密钥/密码泄露检测）
│   ├── raw-sse.ts                     #   原始 SSE 调试工具
│   └── tbox-chat-contract-probe.ts    #   百宝箱聊天契约探针
│
├── package.json                       # 依赖与脚本
├── tsconfig.json                      # TypeScript 严格模式配置
├── next.config.ts                     # Next.js 配置（含 CORS 开发来源白名单）
├── .env.example                       # 环境变量模板
└── .env.local                         # 本地环境变量（不提交 Git）
```

## 数据库模型概要（19 个）

| # | 模型 | 行数 | 说明 | 关键字段/特性 |
|---|------|------|------|-------------|
| 1 | `User` | ~30 | 用户账号 | username (unique), displayName, role, passwordHash |
| 2 | `UserProfile` | ~20 | 用户画像 | targetRole, abilityScores(JSON), educationStage, major, memoryEnabled, onboardingCompleted, version, introStatus |
| 3 | `AuthSession` | ~13 | 认证会话 | tokenHash (unique, SHA-256), expiresAt |
| 4 | `OnboardingConversation` | ~16 | 引导对话 | transcript(JSON), draft(JSON), completeness, status, remoteConversationId |
| 5 | `CareerPlan` | ~24 | 职业计划 V1/V2 | targetRole, schemaVersion (1=V1/2=V2), content(JSON), years/quarters/months(JSON, V1), parentPlanId, sourceReportId, assumptions/riskNotes(JSON) |
| 6 | `ProgressLog` | ~13 | 成长日志 | eventType, title, summary, relatedPlanId, metadata(JSON) |
| 7 | `MemoryItem` | ~18 | 长期记忆 | content, source, sensitivity, status, kind, scope, confidence, reason, expiresAt |
| 8 | `RoleTemplate` | ~18 | 岗位模板 | roleKey (unique), category, abilityWeights(JSON), threeYearPath(JSON), aliases(JSON), sources(JSON) |
| 9 | `ResourceItem` | ~11 | 学习资源 | title, type, roleKey, abilityKey, stage, source, url, estimatedHours |
| 10 | `SimulationSession` | ~18 | 模拟训练 | scenarioKey, transcript(JSON), score, feedback(JSON), turnCount, candidateId (unique) |
| 11 | `ProfileUpdateCandidate` | ~23 | 画像更新候选 | field, oldValue(JSON), newValue(JSON), confidence, patch(JSON), baseProfileVersion, evidenceExcerpt, impactSummary, abilityEvidenceId (unique) |
| 12 | `RoleDraft` | ~15 | 岗位草稿审核 | roleKey, content(JSON), status, reviewerId, sourceReportId (unique) |
| 13 | `ManualAiSample` | ~7 | 手工 AI 样本 | scenario (unique), source, payload(JSON) |
| 14 | `ChatConversation` | ~20 | 聊天会话 | title, status, remoteConversationId, state(JSON), activeTurnId, summary, contextVersion, lastSummarizedMessageId |
| 15 | `ChatMessage` | ~17 | 聊天消息 | role, content, parts(JSON), status, turnId, clientRequestId; 联合唯一约束 (conversationId, clientRequestId, role) |
| 16 | `OperationExecution` | ~13 | 操作执行日志 | operationId, opType, status, result(JSON); 联合唯一约束 (userId, conversationId, clientRequestId, operationId) — 幂等去重 + 原子认领 |
| 17 | `QuestionLedger` | ~15 | 提问账本 | normalizedQuestionKey, profileVersion, questionText, profileField, status; 联合唯一约束 (conversationId, normalizedQuestionKey, profileVersion) — 防重复提问 |
| 18 | `AbilityEvidence` | ~13 | 能力证据 | abilityKey, summary, sourceType, confidence, status; 1:1 关联 ProfileUpdateCandidate |
| 19 | `CareerExplorationReport` | ~16 | 职业探索报告 | roleName, roleKey, content(JSON), sources(JSON), status, executionMeta(JSON) |

## 6 项能力维度

| 维度键 | 中文名 | 说明 |
|--------|--------|------|
| `aiTooling` | AI 理解与工具应用 | AI 工具使用熟练度、Prompt 工程、AI 工作流 |
| `roleFoundation` | 岗位专业基础 | 目标岗位的核心专业知识和理论 |
| `dataAnalysis` | 数据分析能力 | 数据处理、统计分析、可视化、洞察提炼 |
| `businessProduct` | 业务/产品理解 | 行业认知、用户需求分析、产品思维 |
| `communication` | 沟通协作表达 | 跨角色沟通、文档表达、团队协作 |
| `projectPractice` | 项目实践与作品沉淀 | 实际项目经验、作品集积累、复盘能力 |

能力评分范围 0-100，存储在 `UserProfile.abilityScores`（JSON 字段）。

## 核心岗位体系

### 4 个种子职业（seedRoleKeys）

`seedRoleKeys` 仅作初始选项和已知别名匹配，**不作为白名单**，不拒答未知职业：

| Key | 名称 | 类别 | 能力权重侧重 |
|-----|------|------|------------|
| `database_administrator` | 数据库管理员（DBA） | 数据/架构/运维型 | roleFoundation + dataAnalysis |
| `ai_product_manager` | AI 产品经理 | 产品/业务/沟通型 | businessProduct + aiTooling |
| `data_analyst` | 数据分析师 | 数据/分析/决策型 | dataAnalysis + roleFoundation |
| `aigc_operator` | AIGC 内容运营 | 内容/运营/增长型 | aiTooling + projectPractice |

**种子数据说明**：当前 seed.ts 包含 3 个 RoleTemplate（AI 产品经理、数据分析师、AIGC 内容运营），DBA 暂时通过别名识别（`identity.ts` 中定义了 5 个 DBA 别名），无需 RoleTemplate 即可识别用户输入。每个种子岗位在 `identity.ts` 中有多个中文/英文别名。

### 通用职业支持

`resolveRoleIdentity()` 函数支持任意职业方向，流程：

1. **别名匹配**：先在内存种子别名表（SEED_ALIASES，约 20 个别名映射）中匹配
2. **数据库别名扩展**：异步加载 `RoleTemplate.aliases` 动态扩展别名表（内存缓存）
3. **自定义 key 生成**：未知职业通过 `stableCustomRoleKey()` 使用 SHA-256 生成 `custom_<hex12>` 格式的稳定 key
4. **Coverage 标记**：verified_template / knowledge_base_supported / live_research / unverified

## 页面路由与访问控制

| 路径 | 页面 | 权限 |
|------|------|------|
| `/login` | 登录/注册 | 公开 |
| `/` | 聊天首页（ChatHomePage） | 需登录（OPEN_CHAT_ENTRY=true 时不需要完成引导） |
| `/onboarding` | 对话式画像引导 | 需登录 |
| `/dashboard` | 成长仪表盘（指标卡 + 雷达图） | 需登录 + 已完成引导 |
| `/path` | 职业路径 | 需登录 + 已完成引导 |
| `/simulation` | 模拟训练 | 需登录 + 已完成引导 |
| `/resources` | 资源中心 | 需登录 + 已完成引导 |
| `/memory` | 记忆与权限中心 | 需登录 + 已完成引导 |
| `/chat` | AI 聊天 | 重定向至 `/` |
| `/admin` | 管理员岗位草稿审核 | 仅 role=admin |

**路由逻辑**：`/` 入口 → 检查登录 → 未登录重定向 `/login` → 如 `OPEN_CHAT_ENTRY=false` 且未完成引导则重定向 `/onboarding` → 否则渲染 `ChatHomePage`。

## 认证与安全

- **会话机制**：Cookie-based（`careermate_session`），SHA-256 哈希存储，支持过期时间
- **密码存储**：bcryptjs 哈希（salt rounds=10）
- **数据隔离**：所有数据库查询绑定 userId，跨用户不可见
- **用户确认策略**：
  - 画像字段 patch（8 个白名单字段）创建 `ProfileUpdateCandidate`（status=pending），UI 展示确认卡片
  - 记忆提案（`memory_proposal`）需用户手动接受/拒绝
  - 计划草稿（`plan_draft`）需用户确认后写入
  - **AI 不能绕过确认直接修改用户数据**
- **协议边界 fail-closed**：传输模式未知时默认拒绝，不假设兼容
- **Scope 四级上下文隔离**（`context-builder.ts` 动态判定，发给 AI 的数据量）：
  - `career_full`：完整画像 + 计划 + 记忆（活跃任务/等待回答）
  - `career_minimal`：仅目标岗位与简要轮廓（已完成的职业对话）
  - `general_minimal`：最小轮廓（通用对话/新会话/非职业话题）
  - `privacy`：零上下文（隐私/删除/账户相关请求）
- **MCP/插件安全**：userId 和 scopes 仅从服务端环境变量读取，不接受工具参数覆盖
- **百宝箱 API-Key**：仅存服务端环境变量

## 架构特点

### Workspace 模式
- 无全局状态管理库（React/RxJS/Redux），使用 React useState/useEffect + fetch 获取数据
- `workspace.tsx` 统一管理所有数据加载（`/api/me` 聚合端点 + 6 个并行 API 调用）和视图分发
- `workspace-page.tsx` 做鉴权包装，将用户信息和权限注入 Workspace

### 聊天系统
- **ChatHomePage**（`/` 首页）是新的主聊天入口，替代旧的 ChatView（`/chat`）
- **流式输出**：SSE（Server-Sent Events）逐字推送到前端，基于真实百宝箱 WebSocket 事件格式重写解析层
- **有状态轮次**：`STATEFUL_CHAT_TURNS=true` 时启用 —— `turn-service.ts` 管理 begin（幂等检查 + 会话锁）→ 流式处理 → commit（两阶段事务）
- **AgentResponse 协议**：主聊天通过百宝箱 `variables.result` 字段返回结构化 JSON，包含 intent/task/questions/operations/sourceRefs
  - `questions`：最多 1 个待确认问题，通过 QuestionLedger 防重复
  - `operations`：4 种 AgentOperation —— profile_patch / memory_proposal / plan_draft / exploration_report
  - `sourceRefs`：来源引用（knowledge_base / web_search / ai_inference），关联 citations
- **结构化消息部件**：聊天消息支持 `text` / `citations` / `profile_candidate_ref` / `plan_ref` / `exploration_report_ref` / `memory_proposal_ref`
- **提问账本**：`QuestionLedger` 按 `(conversationId, normalizedQuestionKey, profileVersion)` 唯一约束防止 AI 重复提问
- **Operation 原子认领**：`OperationExecution` 表通过 `(userId, conversationId, clientRequestId, operationId)` 联合唯一约束实现操作级幂等去重

### Plan V2 灵活计划
- `schemaVersion=2` 时使用 `content` 字段（JSON），AI 自由定义 horizon/phases/tasks
- `schemaVersion=1` 时使用 `years/quarters/months` 字段（固定 3 年 36 月结构）
- `compatibility.ts` 提供 V2→V1 数组投影用于兼容旧版 UI
- `planDto()` 在 DTO 层自动双读：V2 返回 content + 空 V1 数组，V1 返回 V1 数据

### Zod 全栈校验
- AI 输出经严格 Schema 校验（AgentResponse / Plan V2 / Exploration Report 等），不合格不落库
- Feature Flag 在 `env.ts` 统一管理，所有 Flag 遵循 fail-closed 原则（默认安全值）

### 引用处理
- 实时流式从 `agentic_tool_start` / `agentic_tool_result` SSE 事件提取引用
- 后端截断 + 去重到 max=12（防止 citations 超限导致前端流中断）
- Markdown 内联引用链接（`[^1]` 格式）

## MCP 工具（5 个）

端点：`POST /api/mcp`，JSON-RPC 2.0 协议，支持 `initialize` / `tools/list` / `tools/call` 三个方法。Token 用户绑定（`CAREERMATE_PLUGIN_TOKEN`），scope 白名单控制：

| 工具名 | 用途 | 所需 Scope | 输入参数 |
|--------|------|-----------|---------|
| `profile.read` | 读取当前 Token 绑定用户的安全职业画像 | `profile:read` | 无（空对象） |
| `profile.candidate.create` | 创建需用户确认的画像更新候选（不直接修改正式画像） | `profile:candidates` | field, newValue, confidence, reason, evidenceExcerpt?, impactSummary?, sourceConversationId? |
| `courses.query` | 按职业/能力维度/关键词查询学习资源 | `courses:read` | query?, roleKey?, abilityKey?, limit(1-20) |
| `jobs.query` | 查询已审核的正式职业模板（RoleTemplate） | `jobs:read` | query?, roleKey?, limit(1-20) |
| `progress.update` | 为绑定用户记录一条职业成长进度（ProgressLog） | `progress:write` | eventType, title, summary?, relatedPlanId? |

所有工具通过 `createCareerMateToolRegistry()` 注册，使用通用 `createToolRegistry()` 框架（基于 Zod Schema 校验 + scope 白名单）。

## 百宝箱结构化输出协议（7 类工作流）

主 Agent 通过 `variables.result` 返回结构化 envelope，`type` 字段严格匹配：

| 工作流 | resultType | 说明 | 主要输出 |
|--------|-----------|------|---------|
| 技能评估 | `profile_assessment` | 6 维能力评分、优势、差距、候选更新 | 评分 + ProfileUpdateCandidate[] |
| 画像匹配 | `role_match` | 最多 3 个匹配岗位、得分、差距分析 | RoleMatch[] |
| 职业计划 | `career_plan` | Plan V2 灵活周期计划 + 候选更新 | AiCareerPlanV2 |
| 学习路线 | `learning_route` | 阶段划分、周任务、资源推荐、风险提示 | LearningRoute |
| 模拟训练（轮）| `simulation_turn` | 场景对话轮次、评分指示 | SimulationTurn |
| 模拟训练（报告）| `simulation_report` | 综合评分、优势、改进建议、能力影响 | SimulationReport |
| 简历优化 | `resume_review` | 问题诊断、修改建议、改写示例、事实核查 | ResumeReview |

⚠️ **平台能力缺口**：真实百宝箱 API 的 `conversation.chat.completed` 事件不含 `structured` 字段，因此 `TBOX_STRUCTURED_MODE=terminal` 仅在 mock 模式下可用，生产环境必须为 `disabled`。

## 可用脚本

```bash
# 开发与构建
npm.cmd run dev              # 开发服务器（--webpack 模式，端口 3000）
npm.cmd run build            # 生产构建
npm.cmd run start            # 生产启动

# 测试
npm.cmd run test             # 单元测试（Vitest，84 文件 677 用例，运行约 5s）
npm.cmd run test:watch       # 监听模式持续运行
npm.cmd run test:e2e         # E2E 测试（Playwright，4 spec 43 用例）
npm.cmd run test:migrations  # 数据库迁移冒烟测试

# 代码质量
npm.cmd run lint             # ESLint（--max-warnings=0，零警告策略）
npm.cmd run typecheck        # TypeScript 类型检查（prisma generate → tsc --noEmit）
npm.cmd run secret:scan      # 敏感信息扫描（检测密钥/密码泄露）

# 全量门禁
npm.cmd run verify           # secret:scan + lint + typecheck + test + test:migrations + build

# 数据库
npm.cmd run prisma:generate  # 生成 Prisma 客户端
npm.cmd run db:migrate       # 开发环境创建新迁移
npm.cmd run db:migrate:deploy# 应用已有迁移
npm.cmd run db:migrate:baseline # 标记基线迁移（用于已有数据库）
npm.cmd run seed             # 重新填充演示数据（覆盖已有数据）

# 工具
npm.cmd run tbox:probe       # 百宝箱聊天契约探针（验证真实 API 连通性）
npm.cmd run e2e:serve        # 启动 E2E 测试专用服务器
```

## 文档索引

| 文档 | 说明 |
|------|------|
| `docs/AI职业导航项目方案.md` | 项目整体方案与比赛定位 |
| `docs/产品需求文档_PRD.md` | 详细产品需求文档 |
| `docs/接口设计文档.md` | API 接口设计规范 |
| `docs/tbox/main-agent.md` | 主 Agent 配置（AgentResponse 协议 + Plan V2 + 开放职业支持） |
| `docs/tbox/workflows.md` | 工作流配置（7 类结构化输出模板） |
| `docs/tbox/knowledge-bases.md` | 知识库说明（数据集与检索策略） |
| `docs/tbox/acceptance-evidence.md` | 百宝箱真实 API 验收证据 |
| `docs/tbox/百宝箱配置清单.md` | 百宝箱平台完整配置清单 |
| `docs/evaluation/tbox-cases.md` | 百宝箱评测用例设计 |
| `docs/evaluation/user-testing.md` | 用户测试计划与场景 |
| `docs/evaluation/competition-evidence.md` | 比赛佐证材料汇总 |
| `docs/tbox/2026-07-14-新主Agent适配修复优化计划.md` | 主 Agent 适配历史计划 |
| `docs/tbox/2026-07-15-开放式主聊天与混合记忆实施计划.md` | 开放聊天与记忆系统实施计划 |

## 测试覆盖

### 单元测试（Vitest）
- **84 个测试文件，677 条用例**，全部通过（运行时间约 5s）
- 覆盖范围：所有 `src/lib/` 核心模块、所有 API Route Handler、组件渲染测试
- 重点关注模块：聊天系统（agent-protocol / stream-service / turn-service / service）、百宝箱适配层（adapter / citations / normalization / streaming / sse）、计划服务（compatibility / generation / replan / schema-v2）

### E2E 测试（Playwright）
- **4 个 spec 文件，43 条用例**
- `chat-home.spec.ts`（17 条）：聊天首页全流程（发送消息、流式响应、结构化卡片、快捷操作、记忆提案）
- `p0-flows.spec.ts`（6 条）：P0 核心流程（登录、引导、仪表盘、路径、DBA 全字段验证）
- `unified-shell.spec.ts`（11 条）：统一壳导航（侧栏、移动端菜单、各视图渲染、响应式）
- `chat-context-continuity.spec.ts`（9 条）：上下文连续性（多轮对话、记忆持久化、画像候选确认）

## 当前状态（2026-07-19）

### 已完成 ✅
- ✅ P0 全部功能实现：登录/注册、对话式画像引导、仪表盘、职业路径、模拟训练、资源中心、记忆权限、管理员审核
- ✅ 通用职业支持——任意非种子岗位不拒答，`resolveRoleIdentity()` 别名匹配 + 自定义 key
- ✅ AgentResponse 协议完整实现——结构化 AI 输出（intent/task/questions/operations/sourceRefs），正文 JSON 零副作用
- ✅ Plan V2 Schema 就绪（`schema-v2.ts`），V1 历史兼容读取（`compatibility.ts` 双向投影）
- ✅ 有状态轮次服务（`turn-service.ts`）——幂等 clientRequestId + 会话锁 + 两阶段事务
- ✅ Operation 原子认领（`OperationExecution` 表）——4 种 AgentOperation 幂等去重
- ✅ 提问账本防重复（`QuestionLedger` 表）——按 conversationId + normalizedQuestionKey + profileVersion 唯一约束
- ✅ Scope 四级上下文隔离——career_full / career_minimal / general_minimal / privacy
- ✅ 引用处理——SSE 事件提取 + 去重 + 截断 max=12
- ✅ MCP JSON-RPC 2.0 端点——5 工具完整实现（通用注册框架 + CareerMate 业务注册）
- ✅ 上下文构建器（`context-builder.ts`）——profile/plan/memory scope 控制 + 意图分类
- ✅ 统一 warm-UI——Tailwind v4 + 设计令牌 + 响应式适配（桌面侧栏 / 移动端底部导航 / 抽屉菜单）
- ✅ Markdown 渲染（react-markdown + remark-gfm，memo 优化）+ 移动端响应式适配
- ✅ SSE 流式解析层重写——匹配真实百宝箱 API `conversation.message.add` / `conversation.chat.completed` 事件格式
- ✅ 协议边界 fail-closed——传输模式未知时默认拒绝
- ✅ 隐私数据导出（GDPR 风格 ZIP）+ 清空（强制确认短语校验）
- ✅ E2E 覆盖核心流程（43 条用例）
- ✅ 单元测试覆盖（84 文件 677 用例全绿）
- ✅ ESLint 零警告 + TypeScript 严格模式通过
- ✅ 全量门禁脚本 `verify`：扫描 + lint + 类型 + 测试 + 迁移 + 构建，一步通过

### 待完善 🔧
- 🔧 `AGENT_OPERATIONS_V1` Feature Flag 默认关闭：依赖 TBox structured 输出（terminal 模式），真实 API 平台能力缺口，仅 mock 环境可用
- 🔧 `PLAN_V2_WRITE` Feature Flag 默认关闭：V2 读取已就绪，AI 生成写入需等真实探针验证
- 🔧 `CONVERSATION_SUMMARY` Feature Flag 默认关闭：24 条阈值版本化摘要，需等真实探针验证
- 🔧 DBA 种子数据缺 RoleTemplate：目前仅有别名识别，无预置模板（seed.ts 中 3 个 RoleTemplate，DBA 未包含）
- 🔧 `TBOX_STRUCTURED_MODE` 生产不可用：真实百宝箱 API `conversation.chat.completed` 事件不携带 `structured` 字段，需平台侧支持
- 🔧 真实百宝箱 API 探针验证：部分功能（Agent Operations / Plan V2 写入 / 会话摘要）仍需通过真实 API 探针后才能开启 Feature Flag
- 🔧 处于持续打磨和测试完善阶段，目标比赛初赛冲奖

### 近期重要提交（最近 5 条）
| 提交 | 说明 |
|------|------|
| `d052096` | fix: TBox citations 超限导致前端流中断——截断+去重到 max=12 + artifact 静默跳过 |
| `e66b16d` | feat: 通用职业回归测试——任意非种子岗位无白名单拒答 |
| `d91b052` | fix: 7-in-1 r4——onboarding 通用角色提取、scope 不污染、Operation 原子 claim、PlanV2 负分配、quickActions 去重、通用表达解析、SEED 补全 |
| `0ba0221` | fix: 5-in-1 第三轮验收——OperationJournal 持久化 / Scope 通用模式 / lint 修复 / Plan 比例分配 / E2E 通用断言 |
| `5a66d9e` | fix: 6-in-1 第二轮验收——scope 通用模式 / PlanV2 真实时间轴 / 幂等 outbox / DBA 全字段 E2E / actionId 严格校验 / activePlan 解析 |
