# CareerMate 项目说明书

> 此文件供 AI 助手快速了解项目全貌，无需重复扫描代码库。

## 项目定位

CareerMate 是面向高校生与职场新人的 **AI 职业导航与终身学习伙伴系统**，基于蚂蚁百宝箱企业版 AI。通过对话式画像采集、职业能力评估、3 年路径规划、月度行动计划、职场模拟训练、资源推荐和长期记忆管理，帮助用户将抽象职业目标拆解为可执行、可追踪、可调整的成长路径。

为**浙江大学学生服务创新大赛**开发的本地原型，目标是初赛冲奖。

## 技术栈

| 领域 | 选型 |
|------|------|
| 框架 | Next.js 16 (App Router) |
| 语言 | TypeScript 5.9（严格模式） |
| 前端 | React 19 + Tailwind CSS v4 |
| 字体 | Geist / Geist Mono |
| 图标 | lucide-react |
| 图表 | recharts（能力雷达图） |
| Markdown | react-markdown + remark-gfm |
| ORM/数据库 | Prisma 6 + SQLite（单文件本地数据库） |
| 运行时校验 | Zod 3 |
| 认证 | 自定义 Session Token（bcryptjs 哈希 + cookie） |
| AI 接入 | 蚂蚁百宝箱 API（支持 api/manual/mock 三级降级） |
| 单元测试 | Vitest 3 |
| E2E 测试 | Playwright 1.61 |

## 快速开始

```bash
# 依赖
npm.cmd install

# 环境变量（默认 mock 模式可直接用）
copy .env.example .env.local

# 数据库
npm.cmd run prisma:generate
npm.cmd run db:migrate:deploy
npm.cmd run seed

# 启动
npm.cmd run dev
# → http://localhost:3000
```

## 演示账号

密码均为 `careermate123`：

| 账号 | 显示名 | 身份 | 目标岗位 |
|------|--------|------|---------|
| `student_lin` | 小林 | 大三·数字媒体技术 | AI 产品经理 |
| `student_chen` | 小周 | 大二·统计学 | 数据分析师 |
| `student_wu` | 小陈 | 大三·新闻传播 | AIGC 内容运营 |
| `worker_zhao` | 阿敏 | 职场新人·运营1年 | AI 产品经理（转岗） |
| `career_switch_li` | 宇航 | 新媒体运营 | 数据分析师（摇摆中） |
| `admin` | 管理员 | - | 管理员（审核岗位草稿） |

## AI 模式

- **mock**（默认）：本地确定性数据，无需外部依赖即可运行完整流程
- **api**：真实百宝箱 API，需配置 API-Key
- **manual**：手工样本/静态 Fixture

降级链：`api → manual → mock`

## 项目结构

```
src/
├── app/                          # Next.js App Router 页面与 API 路由
│   ├── page.tsx                  # 首页 / (未登录→登录，未引导→引导)
│   ├── layout.tsx                # 根布局（Geist 字体）
│   ├── globals.css               # 全局样式（Tailwind + 设计令牌 + 聊天样式）
│   ├── login/page.tsx            # 登录/注册
│   ├── onboarding/page.tsx       # 对话式画像引导
│   ├── dashboard/page.tsx        # 成长仪表盘
│   ├── path/page.tsx             # 职业路径
│   ├── simulation/page.tsx       # 模拟训练
│   ├── resources/page.tsx        # 资源中心
│   ├── memory/page.tsx           # 记忆与权限中心
│   ├── chat/page.tsx             # AI 聊天（重定向至 /）
│   ├── admin/page.tsx            # 管理员岗位草稿审核
│   └── api/                      # 全部 API Route Handlers
│       ├── auth/                 # 登录/注册/登出
│       ├── chat/                 # 会话 CRUD + SSE 流式消息
│       ├── plans/                # 职业计划 CRUD + 生成 + 重规划
│       ├── careers/              # 职业探索报告
│       ├── profile/              # 画像候选
│       ├── onboarding/           # 引导对话 + 完成
│       ├── mcp/                  # JSON-RPC 2.0 MCP 端点（5 个工具）
│       ├── tbox/                 # 百宝箱聊天/检索
│       ├── resources/            # 学习资源
│       ├── simulations/          # 模拟训练会话
│       ├── memory/               # 记忆条目
│       ├── admin/                # 角色草稿审核
│       ├── privacy/              # 数据导出/清空
│       └── me/                   # 当前用户聚合数据
│
├── components/                   # 可复用 React 组件
│   ├── chat/                     # 聊天首页、侧栏、输入框、消息渲染、卡片
│   ├── shell/                    # 应用壳（侧边栏、导航、页头）
│   ├── ui/                       # 基础 UI 组件（按钮、卡片、对话框、骨架屏等）
│   ├── workspace.tsx             # 工作台总控制器（数据加载 + 视图分发）
│   ├── workspace-page.tsx        # 工作台页面包装器（鉴权）
│   └── login-form.tsx            # 登录/注册表单
│
├── features/                     # 页面级功能视图（客户端组件）
│   ├── chat/chat-view.tsx
│   ├── dashboard/dashboard-view.tsx  # 指标卡 + 能力雷达图
│   ├── onboarding/onboarding-view.tsx
│   ├── path/path-view.tsx
│   ├── simulation/simulation-view.tsx
│   ├── resources/resource-view.tsx
│   ├── memory/memory-view.tsx
│   └── admin/admin-view.tsx
│
└── lib/                          # 核心业务逻辑
    ├── auth.ts                   # 认证与会话管理
    ├── prisma.ts                 # Prisma 客户端单例
    ├── env.ts                    # 环境变量读取
    ├── dto.ts                    # 数据转换（userDto, profileDto, planDto）
    ├── types.ts                  # 核心类型定义
    ├── workspace-types.ts        # 工作台数据类型
    ├── career.ts                 # 匹配度计算 + 计划构建
    ├── simulation.ts             # 模拟训练逻辑
    ├── path.ts                   # 计划时间线分组
    ├── resources.ts              # 资源来源过滤
    ├── onboarding.ts             # 画像引导逻辑
    ├── privacy.ts                # 隐私数据导出/清理
    ├── ai-runtime.ts             # AI 运行时状态
    ├── client-api.ts             # 前端 API 封装
    ├── markdown-utils.ts         # Markdown 工具
    ├── chat/                     # 聊天服务层
    │   ├── server.ts             # SSE 流式服务器适配
    │   ├── service.ts            # 业务逻辑
    │   ├── repository.ts         # 数据访问层
    │   ├── context.ts            # 意图分类 + 安全上下文
    │   ├── stream-service.ts     # 流式处理
    │   ├── artifact-service.ts   # 结构化卡片生成
    │   ├── artifacts.ts          # 消息部件类型
    │   ├── schemas.ts            # Zod 校验
    │   └── types.ts              # 聊天类型
    ├── tbox/                     # 百宝箱适配层
    │   ├── adapter.ts            # 核心适配器
    │   ├── client.ts             # HTTP 客户端
    │   ├── streaming.ts          # 流式处理
    │   ├── frontend-sse.ts       # 前端 SSE 消费
    │   ├── sse.ts                # SSE 解析
    │   ├── retrieval.ts          # 知识库检索
    │   ├── normalization.ts      # 响应归一化
    │   ├── schemas.ts            # Zod schema
    │   ├── plan.ts               # AI 计划生成
    │   ├── errors.ts             # 错误类型
    │   ├── fixtures.ts           # Mock 数据
    │   └── types.ts              # Tbox 类型
    ├── plans/                    # 计划服务
    │   ├── generation-service.ts
    │   └── replan-service.ts
    ├── profile/                  # 画像服务
    │   ├── candidate-service.ts
    │   └── ability-evidence.ts
    ├── careers/                  # 职业探索服务
    │   ├── exploration-service.ts
    │   └── exploration-schema.ts
    └── tools/                    # MCP 工具注册表
        ├── registry.ts
        └── careermate-registry.ts

prisma/
├── schema.prisma                 # 17 个数据模型
├── seed.ts                       # 种子数据
└── migrations/                   # 数据库迁移

docs/                            # 项目文档
├── AI职业导航项目方案.md
├── 产品需求文档_PRD.md
├── 接口设计文档.md
└── tbox/                         # 百宝箱配置文档

e2e/                             # Playwright E2E 测试
scripts/                         # 辅助脚本
```

## 数据库模型概要（17 个）

| 模型 | 说明 | 关键字段 |
|------|------|---------|
| `User` | 用户账号 | username, displayName, role, passwordHash |
| `UserProfile` | 用户画像 | targetRole, abilityScores, educationStage, weeklyAvailableHours |
| `AuthSession` | 认证会话 | tokenHash, expiresAt |
| `OnboardingConversation` | 引导对话 | transcript, draft, completeness, status |
| `CareerPlan` | 3 年职业计划 | years, quarters, months（均为 JSON 字符串）, version, status |
| `ProgressLog` | 成长日志 | eventType, title, summary |
| `MemoryItem` | 长期记忆 | content, source, sensitivity, status |
| `RoleTemplate` | 岗位模板 | roleKey, abilityWeights, threeYearPath 等 |
| `ResourceItem` | 学习资源 | title, type, roleKey, abilityKey, stage |
| `SimulationSession` | 模拟训练 | scenarioKey, transcript, score, feedback |
| `ProfileUpdateCandidate` | 画像更新候选 | field, oldValue, newValue, confidence, status |
| `RoleDraft` | 岗位草稿 | roleKey, content, status, reviewerId |
| `ChatConversation` | 聊天会话 | title, status, lastMessageAt |
| `ChatMessage` | 聊天消息 | role, content, parts（结构化部件）, status |
| `AbilityEvidence` | 能力证据 | abilityKey, summary, sourceType, confidence |
| `CareerExplorationReport` | 职业探索报告 | roleName, content, sources, status |
| `ManualAiSample` | 手工 AI 样本 | scenario, source, payload |

## 6 项能力维度

| 维度键 | 中文名 |
|--------|--------|
| `aiTooling` | AI 理解与工具应用 |
| `roleFoundation` | 岗位专业基础 |
| `dataAnalysis` | 数据分析能力 |
| `businessProduct` | 业务/产品理解 |
| `communication` | 沟通协作表达 |
| `projectPractice` | 项目实践与作品沉淀 |

## 3 个核心岗位

- **AI 产品经理**（产品/业务/沟通型）
- **数据分析师**（数据/分析/决策型）
- **AIGC 内容运营**（内容/运营/增长型）

## 页面路由与访问控制

| 路径 | 页面 | 权限 |
|------|------|------|
| `/login` | 登录注册 | 公开 |
| `/` | 聊天首页 | 需登录 + 已完成引导 |
| `/onboarding` | 画像引导 | 需登录 |
| `/dashboard` | 成长仪表盘 | 需登录 + 已完成引导 |
| `/path` | 职业路径 | 同上 |
| `/simulation` | 模拟训练 | 同上 |
| `/resources` | 资源中心 | 同上 |
| `/memory` | 记忆权限中心 | 同上 |
| `/chat` | AI 聊天 | 重定向至 `/` |
| `/admin` | 管理员审核 | 仅 admin 角色 |

## 认证与安全

- Cookie-based 会话：`careermate_session`
- 密码：bcryptjs 哈希
- 会话令牌：SHA-256 哈希后存数据库
- 跨用户数据隔离：所有查询绑定 userId
- 画像/计划写入需用户手动确认，AI 不能绕过
- 百宝箱 API-Key 仅存服务端环境变量

## 架构特点

- **无全局状态管理库**：使用 React useState/useEffect + API 获取数据
- **Workspace 模式**：`workspace.tsx` 统一管理所有数据加载和视图分发
- **AI 降级**：api → manual → mock 三级，确保离线可用
- **结构化消息**：聊天消息支持 text / citations / profile_candidate_ref / plan_ref / exploration_report_ref 等部件
- **SSE 流式**：AI 对话使用 Server-Sent Events 逐字输出
- **Zod 全栈校验**：AI 输出经严格 Schema 校验，不合格不落库
- **DTO 模式**：数据库行 → API 响应统一通过 `dto.ts` 转换

## 可用脚本

```bash
npm.cmd run dev              # 开发服务器
npm.cmd run build            # 生产构建
npm.cmd run start            # 生产启动
npm.cmd run test             # 单元测试（Vitest）
npm.cmd run test:e2e         # E2E 测试（Playwright）
npm.cmd run lint             # ESLint
npm.cmd run typecheck        # TypeScript 类型检查
npm.cmd run verify           # 全量门禁（扫描+lint+类型+测试+迁移+构建）
npm.cmd run seed             # 重新填充演示数据
npm.cmd run db:migrate:deploy # 应用数据库迁移
```

## 当前状态

- P0 全部功能已实现
- 已完成统一 warm-UI 迁移
- 已支持 Markdown 渲染
- 移动端响应式已适配
- E2E 测试覆盖核心流程
- 处于打磨和测试阶段
