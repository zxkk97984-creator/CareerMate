# 文档校准与清理记录

日期：2026-09-09。范围仅文档；未修改代码、数据库、平台或已有 .gitignore 改动。未创建提交。

## 更新与合并

- architecture.md：整体按当前源码校准，保留路径，更新校准日期；去掉固定测试数量、固定生成耗时、本机配置默认化及全结果候选化表述。
- README.md：修正快照传输、工作流并非确定性计算、平台记忆边界和 V2 缓冲回复，新增维护入口。
- AGENTIC_V2_HANDOFF.md：将重复且部分过时的长文合并为导航；当前架构放在本地架构概览，平台事实放在百宝箱现状文档。保留旧路径供已有引用使用。
- 接口设计文档.md：修正 /chat 重定向错误及固定 business_data 表述；其余 API 字段未做全量验收。
- 新增 docs/README.md 分类文档状态。

## 删除

| 原文件 | 原因 | 替代 |
|---|---|---|
| docs/superpowers/plans/2026-07-23-public-readme-v2.md | 一次性 README 编写步骤，包含旧分支提交/推送操作和固定 business_data 等已过时假设 | 当前 README、前后端架构、文档导航 |
| docs/superpowers/specs/2026-07-23-public-readme-v2-design.md | 同一 README 重写任务的设计稿，重复当前入口且包含已过时运行描述 | 同上 |

删除前已阅读全文并搜索引用；只有两文件内部互相引用，没有发现其他 Markdown 的链接依赖。两份文件均为 Git 已跟踪历史文件，可从历史恢复。它们不是 ADR，保留其他历史设计及运行证据。

## 验证与限度

本次核对 package.json、layout/chat 页面、WorkspacePage、模块加载 Hook、认证、Prisma 模型、快照传输、V2 缓冲、Artifact 摄入/处理和模拟完成路径。未声称逐个审计全部 Route Handler、前端组件或安全规则。

验证 Markdown 本地链接及 git diff --check；不因纯文档改动运行数据库初始化、付费 AI 调用或全量业务测试。

检查期间发现工作区并发新增 src/lib/chat/stream-service.ts 错误诊断相关修改；本任务未写入或回退该代码。文档中的 TBOX_UNAVAILABLE 为此前截图证据，不代表并发修改后的所有错误都会使用同一错误码。

## 百宝箱目录追加清理

按用户要求删除 `docs/tbox/2026-07-14-新主Agent适配修复优化计划.md` 和 `docs/tbox/2026-07-15-开放式主聊天与混合记忆实施计划.md`。两者是旧实施任务与命令清单，当前源码校准文档及百宝箱现状文档已承担架构说明；不再作为当前执行计划保留。同步更新文档导航与验收记录中的历史文件说明。

保留配置指导、知识库与工作流契约、Prompt 原文、历史审计和验收证据。本次未删除业务代码或平台配置。
