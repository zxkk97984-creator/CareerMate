# 仓库清理记录

日期：2026-09-11。

## 清理范围

- 移出历史 `tasks/`、`docs/superpowers/`、9 月 9–10 日截图验收目录和提交稿/PPT 构建资料；它们不是运行依赖。包含未入 Git 的资料一并归档在项目外 `CareerMate-archive-20260911`，清单为归档根目录的 `cleanup-manifest.json`。共 133 文件、5,448,538 字节。
- 删除无引用的旧 `ChatView`、`GlobalKurisu`/`KurisuChatWindow`、旧聊天侧栏与引用组件、`useSuggestions`、未使用的 field/skeleton/empty-state 组件，以及已由 UnifiedTaskDetail 替代的 TaskDetailPanel 和仅供它使用的 helper/测试。
- 移除无源码引用的 `recharts`，npm 同时移除其 36 个随附包。没有删除当前桌宠资源或升级其他依赖。
- README 精简为功能、启动、架构、命令和维护入口；架构统一为 `docs/architecture.md`，API 文档按当前路由重写。修正模拟训练改版后的旧浏览器测试选择器。
- 忽略 `artifacts/`、`tasks/` 等可再生成的本地产物。原始数据库、环境配置和招聘材料继续保持忽略。

## 保留与验证

保留当前业务功能、历史数据库迁移、当前平台提示词/Skill、资源数据、自动化测试、原始 PRD 与有日期的平台运行证据。兼容 API 仍有调用和诊断价值，不按“旧”字样盲删。历史材料不再作为运行契约。

验收使用 `npm run verify` 和浏览器测试，检查本地 Markdown 链接及提交内容。真实百宝箱上架状态未在本次清理中修改。
