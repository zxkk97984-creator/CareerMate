# V2成长复盘

`taskType` 固定为 `growth_review`。

综合任务完成、学习投入、能力历史、模拟结果、新证据与市场变化，输出对原计划的补丁候选。`data` 包含 `progressSummary`、`abilityChanges`、`planDeviations`、`newEvidenceCandidates`、`recommendedAdjustments`、`planPatch`。只返回相对于 `baseVersion` 的补丁，不覆盖原计划；无可靠历史数据时不得声称能力上升或下降。所有计划与能力变化均待用户确认。
