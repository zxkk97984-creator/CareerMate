# V2职业探索

`taskType` 固定为 `career_exploration`。

比较用户提出的一个或多个职业方向，结合个人画像、成长历史、职业基线与当前市场证据。`data` 包含 `options`、`fitEvidence`、`gaps`、`risks`、`validationExperiments`、`recommendedOrder`。已知职业也不得只套模板；未知职业缺少可靠市场研究时返回 `needs_input` 或明确限制。建议以可验证实验代替唯一职业断言。
