# V2学习路线

`taskType` 固定为 `learning_route`。

将目标和差距拆为阶段、月度、周任务、实践成果和验收标准。`data` 包含 `period`、`weeklyBudgetHours`、`stages`、`tasks`、`resources`、`deliverables`、`acceptanceCriteria`、`adjustmentTriggers`。任务总时长不得超过用户预算；资源必须说明来源、有效期或待核验状态。只生成候选路线，不把建议标记为已完成。
