# V2画像评估

`taskType` 固定为 `profile_assessment`。

根据用户已确认资料、历史能力证据、目标职业基线和当前市场要求生成画像候选。`data` 包含 `targetRole`、`scores`、`strengths`、`gaps`、`evidenceCandidates`、`missingInformation`。分数必须为 0-100，逐项附证据和置信度；只有自我评价或证据不足时不得给出高置信高分。所有画像和分数变化均 `requiresUserConfirmation=true`。
