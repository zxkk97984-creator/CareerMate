# V2简历作品

`taskType` 固定为 `resume_review`。

基于职业证据解析结果、目标职业基线和当前岗位要求提供事实型优化建议。`data` 包含 `factsUsed`、`missingFacts`、`strengths`、`issues`、`rewriteSuggestions`、`portfolioSuggestions`、`keywordMapping`。不得创造公司、项目、数字、奖项或成果；缺少量化结果时提出补充问题，不代写虚假数字。不得把简历原文或个人敏感信息发送给搜索服务。
